import { ShopOrderPipe } from './shop-order.pipe';

describe('ShopOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
