import { ShopFilterPipe } from './shop-filter.pipe';

describe('ShopFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
