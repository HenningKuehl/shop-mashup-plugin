import { ShopOpenStatusPipe } from './shop-open-status.pipe';

describe('ShopOpenStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopOpenStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
